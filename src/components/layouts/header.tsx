import logo from "../../assets/logo.svg";
export default function Header () {
    return (
        <header className={'mt-5 justify-center flex items-center'}>
            <img src={logo} alt=""/>
        </header>
    )
}
